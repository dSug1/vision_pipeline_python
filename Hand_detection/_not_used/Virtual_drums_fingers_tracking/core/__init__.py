"""Portable domain logic — framework-free (no cv2 / mediapipe imports).

This package is the part of the app whose *design and logic* should transfer to a
future mobile rebuild. See spec/Virtual_drums_specification.md (sections 2, 15).
"""
