# `V2` — the yaw lean, trimmed at the quaternion

> **STATUS** · ✅ **SHIPPED** 2026-08-28 (`lean_trim`, gains **0.66 / 0.66**, both tools) — ⛔⛔ **RE-OPENED 2026-08-29: a DOUBLE-COVER defect was found in the shipped trim and FIXED (§11), so a LIVE LOOK IS OWED AGAIN.** ✅ The fix is bit-identical on yaw and roll
> **OWNS** · the correction that removes the yaw lean without touching the turn amount
> **SUB** · HAND · **KIND** · perception · **DEP** · `T6`'s diagnosis, `V1`

---

## 1. What the owner asked for

> *"can I just introduce a matrix multiplicator M(current quaternion rotation): at
> the moment of grab, the multiplication is reset to identity. Then, according to
> the value of quaternion rotation on each of the 4 quaternion axis, the
> multiplicator M multiplies the current quaternion to correct the bias."*

Plus two questions: **what contaminates yaw — roll, pitch, both?** and **is it
depth-dependent?**

## 2. ⭐ THE ARCHITECTURE IS TEXTBOOK, AND THE LITERATURE NAMES IT

Correcting an attitude by **multiplying** a small correction onto a nominal
estimate — rather than adding — is the **multiplicative error quaternion** of the
**MEKF (Multiplicative Extended Kalman Filter)**, standard in satellite attitude
determination and inertial navigation, for exactly the reason it is right here: a
multiplicative update preserves the unit-norm constraint where an additive one
does not. *"Reset to identity at grab"* is the same nominal-state +
multiplicative-correction split, and it composes cleanly with the cube's rotation
already being grab-relative.

⛔ **What the literature CHANGED: swing/twist, not axis-component scaling.** The
first draft scaled the rotation axis's off-vertical components and renormalised —
ad-hoc. The standard tool factors `q = q_swing ⊗ q_twist` about a chosen axis.
Taken about the vertical, **the twist IS the yaw and the swing IS the
contamination**, exactly. Trimming the swing leaves the turn AMOUNT untouched,
which matters because `ORIENTATION_DIAGNOSIS.md` measured the amount as already
fine (gain 1.13) and the **uprightness** as the thing that fails.

⛔ **MANO-based orientation refinement is the mainstream remedy in the literature
and is licence-blocked by `N13`.** It stays out.

## 3. The two questions, MEASURED (`analysis/lean_decomposition.py`)

**Contaminated by BOTH**, pitch ~1.3× roll, over 7511 yaw-like frames:

| off-axis component | mean | mean abs | character |
|---|---|---|---|
| `nx` → toward **pitch** | +0.375 | **0.431** | one-directional bias (0.87) |
| `nz` → toward **roll** | +0.300 | **0.323** | one-directional bias (0.93) |

⭐ The load-bearing part is not the ratio but that **both are one-directional
BIASES relative to the turn direction**, not symmetric noise. A bias is what a
deterministic correction can remove; noise is what killed the three predecessors.

⛔ **NO depth dependence.** Binned **within** each take, the four takes disagree on
the sign of the trend for both axes. ⚠ An earlier pass pooled the takes and *then*
binned — confounding depth with take identity — and duly reported roll as
depth-dependent on numbers that were not even monotone. **Two pooling errors were
made in one session**; the harness now judges within-take by construction.

## 4. ⛔⛔ THE REGRESSION THAT COST A LIVE SESSION — and the owner found it

The correction faded in on the **twist MAGNITUDE** (`min(1, |twist|/15°)`). It
passed its golden vectors, which used **mathematically pure** pitch and roll where
the twist is exactly zero. **A real hand never produces that**: a pitch gesture
carries incidental yaw, cleared the 15° ramp, and had its entire swing damped.

> Owner: *"that creates an issue because pitch and roll are heavily damped as a
> consequence."*

⭐ **The fix is to ask a RATIO, not a magnitude** — how much of *this* rotation is
yaw?

    r = |twist| / (|twist| + |swing|)        0 = pure pitch/roll, 1 = pure yaw
    w = ramp(r, 0.35 → 0.65)

Measured over 9880 rotating frames: yaw-dominant frames sit at **r = 0.805**,
pitch/roll-dominant at **0.191** — barely overlapping. A `pitch 40° + yaw 10°`
gesture is now **byte-unchanged at gain 1** (`6.4e-15°`), and that is a golden
vector so it cannot regress.

⭐⭐ **THE METHOD RULE: a golden vector built from a MATHEMATICALLY PURE input
tests a case the product never sees.** The vectors were green on a build the owner
rejected in one minute. Purity is the wrong idealisation when the defect lives in
the impurity.

## 5. ⛔ The owner's other proposal, measured and declined

> *"can the damp be connected to the hand ratios (the 6 takes we discarded
> previously)…?"*

Tested `edge_on_measure` — `T6`'s one **scale-free** survivor, so not subject to
CAVEAT ZERO — as the discriminator:

| candidate | yaw-dominant | pitch/roll-dominant | separation | p95 jitter |
|---|---|---|---|---|
| twist ratio | 0.805 | 0.191 | **0.613** | 0.126 |
| `edge_on_measure` | 0.729 | 0.651 | **0.078** | 0.199 |

**8× worse at separating and 1.6× noisier.** ⭐ And it is a 2-D SHAPE feature —
the exact class whose VARIANCE killed the three estimators in `REJECTED.md`. The
twist ratio reads only the quaternion, so it adds no new noise source at all.

## 6. ⛔ THE GATE — cleared on 3 of 4 takes, and the fourth is stated openly

`REJECTED.md`: *"demonstrate a per-frame orientation jump at or under shipped
Horn's on a GRABBING take BEFORE quoting a lean number."*
`analysis/lean_trim_ab.py`, p95 jump ratio at the shipped 0.66/0.66:

| take | ratio |
|---|---|
| `stripped` | **1.072×** ⚠ over |
| `steadytrans` | 0.998× |
| `frob` | 0.987× |
| `freeze` | 0.908× |

⚠ **One take is 7% over the 1.000× bar, and has been the outlier at every gain
since 0.10.** ⭐ For scale: the three rejected predecessors never came within
**1.8×**. This is a different regime — but *better than what was rejected is not
the gate*, and the owner shipped knowing this.

⛔ **The lean numbers the harness prints are SELF-MEASURING** — the metric is the
swing and the trim removes the swing, so at gain 1 it reads 0.0 by construction
(`B4`; the flaw that retracted §16.14's SINK). Only the jump ratio is evidence.

## 7. ⭐⭐ WHY 0.66 — a bracket, not a derivation

The owner set it by eye. Checked afterwards against numbers measured **before this
row existed**. `ROLL` is the one axis that never touches world `z`, so its **6.7°**
mean-axis error is the pipeline's accuracy floor; the lean at a 60–90° turn is
**26.8°**:

    gain landing exactly on the floor = 1 - 6.7/26.8 = 0.750

| gain | residual lean | |
|---|---|---|
| 0.11 | 23.9° | leaves the bias in |
| **0.66** | **9.1°** | **just above the floor** |
| 0.75 | 6.7° | at the floor |
| 1.00 | 0.0° | below it — correcting noise |

⛔ **THE FLOOR IS NOT ONE NUMBER.** The lean grows faster than the turn, so the
floor gain rises with turn size: **0.015 at 20°, 0.455 at 40°, 0.694 at 60°, 0.750
at 75°.** A single scalar cannot sit on it everywhere; 0.66 tracks the large-turn
end, which is where the defect is visible.

⚠ **AND A FIRST MODEL WAS REFUTED BY THE OWNER'S EYE.** Least squares on a
bias-plus-genuine-tilt model gives `g = σ²_bias/(σ²_bias+σ²_genuine) = 0.114`. That
assumed the swing's VARIATION is genuine tilt worth keeping. Tested: its
frame-to-frame change over its spread is **0.11**, not ~1.4 — so it is **not white
noise**, it is a persistent pose-dependent property, and the diagnosis says world
`z` is fabricated *as a function of pose*. ⭐ So the variation is artifact too,
which is exactly why the right gain is far above 0.114.

⚠ **What cannot be claimed**: 6.7° is a mean-axis *error* and the residual lean is
a systematic *tilt* — related, not identical. The argument brackets the answer to
roughly **0.5–0.75**; it does not single out 0.66. ⛔ One route was tried and
closed: using the card takes to estimate genuine tilt — `REJECTED.md` records the
card *reads tilt higher* (17–19° vs 12.6–13.0°) and must never size an axis.

## 8. What shipped

`Resources/lean_trim.py`, stdlib-only/numpy-free/clock-free, in **both tools** at
the same parity anchor, reading **one** set of constants (`L1`: a tuning constant
lives in exactly one module — the debug sliders write the shared module's values,
which is what keeps `parity_replay` meaningful).

* gains **0.66 / 0.66**; **gain 0 returns the input object unchanged**, so the
  `A10` baseline is still reachable and bit-exact.
* Golden vectors: `analysis/verify_lean_trim.py` — the swing/twist identity, the
  perpendicularity of the swing, gain-0 exactness, a pure yaw untouched at any
  gain, a real (impure) pitch untouched, and continuity.
* `analysis/lean_trim_ab.py` (the gate) · `analysis/lean_decomposition.py` (the
  two questions).

## 9. ⚠ What is owed

⛔ **The owner's live look in PRODUCTION.** It was settled in the debug tool over
19 homed trials; production now applies the same constants but has not been run
with them. `METHOD.md`: a live look in **both** tools is what closes a change.

⭐ **✅ CLOSED — see §10.**

## 10. ✅✅ 2026-08-28 — THE PRODUCTION LIVE LOOK, AND THE ROW IS FULLY CLOSED

> **Owner, after running production:** *"production run was done by me and it is
> ok"* — confirmed on asking to cover the yaw-lean trim as well as the assembly.

⛔ **What this closes, precisely:** `V2` was already SHIPPED on the debug tool's
19 homed trials; what stood open was `METHOD`'s requirement that a live look in
**both** tools closes a change. Production applies the same `lean_trim` constants
(0.66 / 0.66) through the same shared module, and the owner has now run it and
accepted it. **Nothing is owed on this row.**

⚠ **What it does NOT claim.** The verdict is *the lean is acceptable in
production*, not a measurement. The gate's numbers are unchanged: it is cleared
on **3 of 4 takes**, `stripped` still sits at **1.072x**, and the owner shipped
knowing it. ⭐ A live acceptance is not a re-measurement, and §7's bracket
(0.5–0.75) still does not single out 0.66 — it never did.

⚠ Also unchanged: the harness's LEAN numbers remain SELF-MEASURING (`B4`), so
only the jump ratio is evidence, and a future revisit must start from that and
not from this acceptance.

## 11. ⛔⛔ 2026-08-29 — A DEFECT IN THE SHIPPED TRIM: THE QUATERNION DOUBLE COVER

> **Found while answering a question that was not about it.** The owner asked
> *"is there a min hand orientation in yaw and pitch for them to be reliable?"* and,
> on the pitch answer, *"doesn't pitch behave like yaw … due to the multiplicator
> matrix?"* ⭐ The second question was right, and following it found the bug.

### What was wrong

`twist_angle_deg` walked the long way round whenever the quaternion carried the
negative sign of the double cover:

```
 15 deg pure yaw                 ->   15.00 deg   (right)
 the SAME rotation, negated quat -> -345.00 deg   (wrong)
```

`q` and `-q` are the **same rotation**, and `horn_rotation` returns whichever sign
its largest-eigenvalue eigenvector happens to carry.

### ⛔⛔ Why it was not cosmetic

`yaw_dominance` divides by that angle. A pitch gesture whose quaternion came back
negated scored `tw = 345` instead of `15`, giving dominance ~**0.99** instead of
~**0.2** — so `authority` went to **1.0 on a gesture that must receive NO
correction at all**. ⭐⭐ And because **a pitch is entirely SWING** in a
decomposition about the vertical, the trim then **shrank the very gesture it was
built to leave alone**.

⭐ The starkest single number, from the golden vector added for it:

| | `authority` on a NEGATED pure 30° pitch |
|---|---|
| before | **1.000** — full correction |
| after | **0.000** — correct |

### Measured, on the corpus

| | |
|---|---|
| `2026-08-02_191816_pitch_sweep_slow` | **23% of frames** carried a corrupted authority; **153** wrong by more than 0.5 |
| that take's mean authority | **0.260 against a true 0.050** — firing **5x** too often |
| its p95 orientation-jump ratio | **1.166x** (a clear fail of this row's own gate) → **1.031x** with the fix |
| `2026-08-04_054702_pitch_sweep_slow` | 1.026x → 1.022x |
| ⭐ **yaw take** | **0.892x → 0.892x — BIT-IDENTICAL** |
| ⭐ **roll take** | **0.995x → 0.995x — BIT-IDENTICAL** |

⭐⭐ **The fix cannot regress what the owner accepted live.** Yaw and roll never
reach the wrap, so they are untouched; only frames that were already being
mis-scored can change.

### Where the bug bit, and where it did not

⚠ **It is NOT arbitrary at the grab**, which was the first hypothesis and it was
wrong. `Horn.freeze()` sets `seed: None`, so the first `delta()` after a grab has
no sign continuity to inherit — but tested across **61 simulated grabs on a real
take, 0 came back negative**. The eigenvector resolves positive near identity in
practice. ⭐ Recorded because it is the more frightening failure and it does not
happen: the correction is not randomised at every grab.

⛔ It bit **during** a grab instead: **21 of 61 grabs (34%)** crossed the wrap
within the following 60 frames, once the fitted rotation passed a half turn.

### The fix

`twist_angle_deg` canonicalises to `w >= 0` before extracting, so `2*atan2(d, w)`
lands in `[-180, 180]` — the shortest arc, which is what every caller wants. One
module, so **both tools** are fixed by it (`N6`). ✅ 44/44 suites,
`analysis/verify_lean_trim.py` extended (§8b, below).

### ⚠⚠ WHY NOTHING CAUGHT IT — and the rule it cost

* **Every golden vector built its quaternions with `quat()`, which always returns
  `w >= 0`.** The suite knew about the double cover in its comparison HELPER
  (`angle_between`) and **never fed a negated quaternion IN**. So it exercised only
  the canonical representation, which is not the only one the product produces.
* ⛔ **`parity_replay` is blind to this class by construction** — both tools import
  this one module, so they were wrong *identically* and agreed perfectly. Parity
  proves the two tools match; it can never prove either is right.
* ✅ The new vectors were checked to **FAIL against the pre-fix code, 6/6** — a
  vector that passes either way catches nothing, and three vectors in the `AS` row
  had already passed for the wrong reason this month.

⭐⭐⭐ **THE METHOD RULE: A GOLDEN VECTOR MUST FEED THE REPRESENTATIONS THE PRODUCT
ACTUALLY PRODUCES, NOT ONLY THE CANONICAL ONE.** `q` and `-q` are the same rotation
to the renderer and *not* to any function that reads an angle out of them.

### ⛔ What this re-opens

**A live look is OWED again on this row.** The change is bit-identical on yaw and
roll, and the pitch takes are instructed open-hand sweeps (`T6`'s rule cuts both
ways — such a corpus cannot condemn a build any more than it can validate one).
But `METHOD` is explicit that only a live look in both tools closes a change, and
this changes shipped behaviour on pitch.
