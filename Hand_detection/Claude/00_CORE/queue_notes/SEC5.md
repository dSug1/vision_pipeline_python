<!-- PROVENANCE — machine-extracted, NOT edited.
     source : Claude/PART_ONE.md lines 1252-1252
     commit : 3d44c9a
     when   : 2026-08-25 documentation reorganisation
     Every byte between the VERBATIM markers below is exactly as it was.
     The map of the new folder layout is Claude/README.md.
-->
<!-- VERBATIM-BEGIN -->
| **SEC5** | **Both tools feed MediaPipe a FAKE clock (`timestamp_ms += 33`)** | perception | **OPEN 2026-08-25 — needs a LIVE two-detector A/B. ⚠ The corpus cannot test it** | — | Both capture loops advance MediaPipe's VIDEO-mode timestamp by a hardcoded 33 ms (30 fps) while **N7 measured the real rate at 15–24 fps** and proved it camera-bound, so the clock MediaPipe is given runs ~1.5–2× fast against the wall. ⭐ The two tools MIRROR each other here, so it is **not** a divergence, and the timestamps are monotonic so MediaPipe's contract is satisfied — nothing is dropped and nothing crashes. ⛔⛔ **WHAT THE EFFECT IS, IS UNKNOWN — AND AN EARLIER VERSION OF THIS ROW OVERSTATED IT (corrected 2026-08-25, same day).** It read *"the tracker is told the hand moves 2× faster than it does, a plausible contributor to landmark jitter"*. **That is a hypothesis about MediaPipe's internals, not a measurement**: in the Tasks API the VIDEO timestamp is primarily a graph packet timestamp, and it is NOT established that the hand-landmarker graph does any velocity- or time-based filtering that would consume it. The honest statement is: the clock is wrong, the output effect is **unmeasured, and may well be nil**. ⚠ Recorded as a correction rather than quietly edited, because this project's own standard is that a plausible mechanism must not become a recorded fact. ⛔⛔ **AND THE CORPUS CANNOT SETTLE IT — THIS IS THE PRACTICAL BLOCKER, NOT EFFORT.** Testing a change to MediaPipe's INPUT means re-running MediaPipe, and **the corpus holds no image data at all** (415 files, landmarks only, deliberately). There is nothing to replay. ⭐⭐ **BUT A CLEAN TEST EXISTS THAT NEEDS NO PIXELS AND NO NEW RECORDING FORMAT**: build **TWO `HandLandmarker` instances in the debug tool** and hand both the SAME `mp_image` every frame — one with `+= 33`, one with the measured `tCapture`. Same camera, same frames, one variable, each detector keeping its own tracking state. That is exactly the multi-arm pattern `update_hands_all` already implements, the extra inference is free (the pipeline is camera-bound, not compute-bound), and both arms record through the existing recorder so `t5h` scores the jitter with no new harness. ⭐ **A NULL RESULT IS WORTH HAVING**: it closes a standing "we should probably fix this someday" item permanently, which is the same reason 2.3's five nulls were kept. ⚠ **Interaction with `F1`, stated so it is not discovered late**: F1 is about *subtle fingertip* motion, so it is the build that would benefit most if this turns out to matter — and the one whose measurements would be hardest to interpret if it does and nobody knew. ⛔ Do NOT block F1 on it; a ~30-minute experiment before F1 is defensible and is the owner's call |
<!-- VERBATIM-END -->
