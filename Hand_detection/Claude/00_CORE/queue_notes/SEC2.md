<!-- PROVENANCE — machine-extracted, NOT edited.
     source : Claude/PART_ONE.md lines 1249-1249
     commit : 3d44c9a
     when   : 2026-08-25 documentation reorganisation
     Every byte between the VERBATIM markers below is exactly as it was.
     The map of the new folder layout is Claude/README.md.
-->
<!-- VERBATIM-BEGIN -->
| **SEC2** | **Pin the DEPENDENCY TREE, not just the two direct deps** | infra / shipping | ⭐ **PART ONE DONE 2026-08-25 (the environment is now RECORDED); the hash-pinned lock is deferred to PACKAGING** | U10/U11 | ⭐⭐ **THE ROW'S ORIGINAL FRAMING WAS SLIGHTLY WRONG AND THE MEASUREMENT CORRECTED IT.** The urgent risk here is **not** a supply-chain attack (one developer, one machine, no CI, deps installed once) — it is **reproducibility of the measurement rig**, and it is not hypothetical: **26 packages are installed, 24 of them transitive from mediapipe and completely unpinned, and they have ALREADY drifted past what mediapipe 0.10.14 was built against — `numpy 2.4.6` and `opencv-contrib-python 5.0.0.93`, both a major version ahead of that release's era.** So rebuilding the `.venv` today does not reproduce the environment the corpus's numbers came from, and nothing recorded what that environment was. ⭐ **DONE NOW, one minute, zero risk: `requirements.lock.txt`** — a `pip freeze` of the working set, committed as a RECORD. ⛔ `launch.bat` does **not** read it: a hash-pinned first-run install that fails hard is worse on a dev machine than a floating one. ⭐ **The reassuring half, and why the product risk is smaller than it looks**: numpy appears in **18 files and every one is a measurement harness** — the shipping path is numpy-free BY CONTRACT (that is what makes it portable), so this threatens the RIG, not the game. ⚠ **STILL TO DO, AT PACKAGING**: a genuinely hash-pinned install (`pip install --require-hashes`) and the **licence inventory generated from it — which is what N13 actually requires**, since you cannot licence-audit a dependency set nobody has enumerated. ⚠ Pair with **U11** (16 MB of dead model files to strip). ⚠ Note for that pass: `jax`, `jaxlib`, `matplotlib` and `sounddevice` are all pulled in by mediapipe and imported by **zero** project files — worth knowing before anyone tries to explain why the install is large, or why an audio package is present in a camera app |
<!-- VERBATIM-END -->
