node_modules/
dist/

# Regenerated by `npm install` (postinstall) / `npm run copy-mediapipe-assets`
# from node_modules and the Python pipeline's model file — not source.
public/mediapipe/
public/models/
