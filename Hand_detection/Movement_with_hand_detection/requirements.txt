# Vision Pipeline — Python dependencies (ORIGINAL server-client system, parked in _not_used)
# Used by MouseCursorApp + its sibling ../Python_Server_MediaPipe_vision_pipeline.
# launch.bat builds a local .venv here from this file. Tested with Python 3.11.

# NOTE: pinned, do not bump blindly. MediaPipe 0.10.30+ removed the legacy
# `mediapipe.solutions` and `mediapipe.framework` Python modules (tasks-only
# builds). The vision server's preview drawing in
# ../Python_Server_MediaPipe_vision_pipeline/Resources/hands_visualizer.py and
# facevisualizer.py still depend on those modules, so we pin to a version that
# ships both the legacy `solutions` API and the newer `tasks` API.
#
# OpenCV (cv2) is provided transitively by mediapipe via `opencv-contrib-python`.
# Do NOT also list `opencv-python` here — installing both ships two copies of the
# `cv2` module and is a common cause of a broken cv2 import after a reinstall.
mediapipe==0.10.14

# Part Zero (see Claude/PART_ZERO.md): local window used to prove out
# "landmark -> object position" before porting to the browser.
pygame==2.6.1
