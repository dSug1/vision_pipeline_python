# Auto detect text files and perform LF normalization
* text=auto

# Windows batch files must stay CRLF (LF breaks cmd.exe multi-line blocks)
*.bat text eol=crlf
*.cmd text eol=crlf
# Shell scripts stay LF
*.sh text eol=lf
